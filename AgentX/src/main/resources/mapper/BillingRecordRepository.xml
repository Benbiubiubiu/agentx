<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xhy.domain.billing.repository.BillingRecordRepository">
    
    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, user_id, product_id, rule_version_id, price_rule, total_amount, amount_left,
        created_at, updated_at, deleted_at
    </sql>

    <!-- 分页查询账单记录 -->
    <select id="queryRecords" resultType="org.xhy.domain.billing.entity.BillingUsageRecordEntity">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM billing_usage_record
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            AND deleted_at IS NULL
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- 查询总记录数 -->
    <select id="countRecords" resultType="long">
        SELECT COUNT(*)
        FROM billing_usage_record
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            AND deleted_at IS NULL
        </where>
    </select>

    <!-- 创建账单记录 -->
    <insert id="createRecord" parameterType="org.xhy.domain.billing.entity.BillingUsageRecordEntity">
        INSERT INTO billing_usage_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            id,
            <if test="userId != null">user_id,</if>
            <if test="productId != null">product_id,</if>
            <if test="ruleVersionId != null">rule_version_id,</if>
            <if test="price_rule != null">price_rule,</if>
            <if test="totalAmount != null">total_amount,</if>
            <if test="amountLeft != null">amount_left,</if>
            <if test="createdAt != null">created_at,</if>
            <if test="updatedAt != null">updated_at,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{id},
            <if test="userId != null">#{userId},</if>
            <if test="productId != null">#{productId},</if>
            <if test="ruleVersionId != null">#{ruleVersionId},</if>
            <if test="price_rule != null">#{price_rule},</if>
            <if test="totalAmount != null">#{totalAmount},</if>
            <if test="amountLeft != null">#{amountLeft},</if>
            <if test="createdAt != null">#{createdAt},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
        </trim>
    </insert>

</mapper> 